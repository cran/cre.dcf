% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simple_api.R
\name{deal_spec}
\alias{deal_spec}
\title{Define a simplified CRE deal specification}
\usage{
deal_spec(
  price,
  horizon_years = 10L,
  entry_yield = NULL,
  noi_y1 = NULL,
  rent_sqm = NULL,
  area_sqm = NULL,
  lease_roll = NULL,
  vacancy_rate = 0,
  opex_sqm = 0,
  purchase_year = as.integer(format(Sys.Date(), "\%Y")),
  index_rate = 0.02,
  acq_cost_rate = 0.06,
  discount_rate = 0.08,
  capex = 0,
  exit_yield_spread_bps = 0,
  exit_cost = 0.015,
  debt = debt_terms()
)
}
\arguments{
\item{price}{Numeric scalar greater than 0. All-in acquisition price
(\code{price_di}) used by the simplified API.}

\item{horizon_years}{Integer scalar greater than or equal to 1.}

\item{entry_yield}{Optional numeric scalar in \code{(0, 1]}. Entry yield for
the top-down mode.}

\item{noi_y1}{Optional numeric scalar greater than 0. Year-1 NOI.}

\item{rent_sqm}{Optional numeric scalar greater than or equal to 0. Annual
rent per sqm.}

\item{area_sqm}{Optional numeric scalar greater than 0. Lettable area in sqm.}

\item{lease_roll}{Optional object returned by \code{\link[=lease_roll]{lease_roll()}}. Use this mode
to describe several units and lease events without writing YAML manually.}

\item{vacancy_rate}{Numeric scalar in \code{[0, 1)}. Used only in the
rent/surface mode.}

\item{opex_sqm}{Numeric scalar greater than or equal to 0. Used only in the
rent/surface and lease-roll modes.}

\item{purchase_year}{Integer scalar. Defaults to the current year.}

\item{index_rate}{Numeric scalar in \code{[0, 1]}. Annual growth/indexation.}

\item{acq_cost_rate}{Numeric scalar in \code{[0, 1)}. Acquisition cost rate.}

\item{discount_rate}{Numeric scalar in \code{[0, 1]}. Discount rate mapped to
the \code{"risk_premium"} engine block.}

\item{capex}{Numeric scalar or numeric vector of length \code{horizon_years}.}

\item{exit_yield_spread_bps}{Numeric scalar. Exit-yield spread in basis
points.}

\item{exit_cost}{Numeric scalar in \code{[0, 1)}. Exit cost rate.}

\item{debt}{Object returned by \code{\link[=debt_terms]{debt_terms()}}.}
}
\value{
An object of class \code{cre_deal_spec}.
}
\description{
Builds a beginner-friendly deal object that can be analyzed with
\code{\link[=analyze_deal]{analyze_deal()}}. Exactly one income mode must be provided:
\itemize{
\item \code{entry_yield},
\item \code{noi_y1},
\item \code{rent_sqm + area_sqm},
\item \code{lease_roll}.
}
}
